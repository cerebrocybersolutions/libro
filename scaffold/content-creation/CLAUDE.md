# Content Creation — Dept CLAUDE.md (starter)

*Dept-scoped front door for the Content Creation project. Overrides workspace-root `CLAUDE.md` for content sessions.*

*Last updated: (operator: populate on first edit).*

---

## Dept folder layout

```
content-creation/
├── CLAUDE.md            # this file — dept front door
├── brain/
│   ├── sessions/
│   ├── decisions/
│   └── pipeline/current-state.md
└── (operator: add sub-folders per channel — e.g. scripts/, recorded/, published/)
```

(operator: populate — adjust layout to match your dept's actual structure)

---

## Mission

(operator: populate — one paragraph. What channels do you publish on? What audience are you growing? What is the role of content relative to the rest of the business — top-funnel, brand, education?)

## Current state

(operator: populate — 3-6 bullets. What is in build? What is published this week? What channels are dormant? What is blocked and on whom?)

## Canonical files for this dept

- `content-creation/brain/sessions/YYYY-MM-DD-content.md` — session notes
- `content-creation/brain/decisions/decisions.md` — decision log (top-append, newest-first)
- `content-creation/brain/pipeline/current-state.md` — live publication pipeline
- (operator: add script-archive path, asset-library path, brand-guidelines doc)

## Dept non-negotiables (beyond root)

1. (operator: populate — example: "No publish without a brand-guidelines pass.")
2. (operator: populate — example: "Every script lands in pipeline state before recording.")

## Active skills

- (operator: list the skills this dept actively uses — keep aligned with mission-control/skills/)

## When to escalate out of Content

- **To the human operator:** on-camera asks, brand-voice calls, anything that puts the operator's face on a channel
- **To Ops:** (operator: populate — example: "Any change to the publication cadence")
- **To other depts:** (operator: populate — which depts feed content topics?)

## Known gotchas

- (operator: populate — fill this in as you accumulate them; one line per gotcha is enough)

## Cross-dept notes

- (operator: populate — which depts are upstream/downstream of this one?)

<!-- AUTOLINK-START — Obsidian wikilink graph backfill (idempotent; safe to re-run) -->

## References (auto-wikilinks)

- [[YYYY-MM-DD-content]]
- [[current-state]]
- [[decisions]]

<!-- AUTOLINK-END -->
