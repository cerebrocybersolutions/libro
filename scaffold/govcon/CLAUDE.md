# GovCon — Dept CLAUDE.md (starter)

*Dept-scoped front door for the GovCon (government contracting) project. Overrides workspace-root `CLAUDE.md` for GovCon sessions.*

*Last updated: (operator: populate on first edit).*

---

## Dept folder layout

```
govcon/
├── CLAUDE.md            # this file — dept front door
├── brain/
│   ├── sessions/
│   ├── decisions/
│   └── pipeline/current-state.md
└── (operator: add sub-folders as your pipeline matures — e.g. opportunities/, vendors/, submissions/)
```

(operator: populate — adjust layout to match your dept's actual structure)

---

## Mission

(operator: populate — one paragraph. What contracts are you pursuing? What set-aside posture do you operate under? What is the line between sourcing, bidding, and delivery?)

## Current state

(operator: populate — 3-6 bullets. What is in flight? What is awarded? What is queued? What is blocked and on whom?)

## Canonical files for this dept

- `govcon/brain/sessions/YYYY-MM-DD-govcon.md` — session notes
- `govcon/brain/decisions/decisions.md` — decision log (top-append, newest-first)
- `govcon/brain/pipeline/current-state.md` — live pipeline state
- (operator: add tracker spreadsheet path, vendor-quote folder, submission archive path)

## Dept non-negotiables (beyond root)

1. (operator: populate — example: "Every solicitation goes through the same scoring sheet before time is spent on a quote.")
2. (operator: populate — example: "Vendor outreach goes out only after a quote-template is filed in the opportunity folder.")

## Active skills

- (operator: list the skills this dept actively uses — keep aligned with mission-control/skills/)

## When to escalate out of GovCon

- **To the human operator:** customer calls, sign-offs, teaming agreements, anything relationship-bearing
- **To Ops:** (operator: populate — example: "Any change to the scoring rubric")
- **To other depts:** (operator: populate — which depts deliver against awarded contracts?)

## Known gotchas

- (operator: populate — fill this in as you accumulate them; one line per gotcha is enough)

## Cross-dept notes

- (operator: populate — which depts are upstream/downstream of this one?)

<!-- AUTOLINK-START — Obsidian wikilink graph backfill (idempotent; safe to re-run) -->

## References (auto-wikilinks)

- [[YYYY-MM-DD-govcon]]
- [[current-state]]
- [[decisions]]

<!-- AUTOLINK-END -->
