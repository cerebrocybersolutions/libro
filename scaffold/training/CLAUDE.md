# Training — Dept CLAUDE.md (starter)

*Dept-scoped front door for the Training project. Overrides workspace-root `CLAUDE.md` for training sessions.*

*Last updated: (operator: populate on first edit).*

---

## Dept folder layout

```
training/
├── CLAUDE.md            # this file — dept front door
├── brain/
│   ├── sessions/
│   ├── decisions/
│   └── pipeline/current-state.md
└── (operator: add sub-folders per course or cohort — e.g. curriculum/, materials/, cohorts/)
```

(operator: populate — adjust layout to match your dept's actual structure)

---

## Mission

(operator: populate — one paragraph. What do you teach, to whom, in what format? Is training internal (employee enablement) or external (customer-facing courses)? What is the line between materials, delivery, and certification?)

## Current state

(operator: populate — 3-6 bullets. What courses are running? What is in build? What is queued? What is blocked and on whom?)

## Canonical files for this dept

- `training/brain/sessions/YYYY-MM-DD-training.md` — session notes
- `training/brain/decisions/decisions.md` — decision log (top-append, newest-first)
- `training/brain/pipeline/current-state.md` — live course pipeline
- (operator: add curriculum master, materials library, cohort roster)

## Dept non-negotiables (beyond root)

1. (operator: populate — example: "Every module ships with a quiz + answer key, never one without the other.")
2. (operator: populate — example: "Curriculum versioning is locked at cohort start.")

## Active skills

- (operator: list the skills this dept actively uses — keep aligned with mission-control/skills/)

## When to escalate out of Training

- **To the human operator:** instructor calls, cohort kickoffs, certification sign-offs
- **To Ops:** (operator: populate — example: "Any change to delivery format")
- **To other depts:** (operator: populate — which depts source learners or use trained output?)

## Known gotchas

- (operator: populate — fill this in as you accumulate them; one line per gotcha is enough)

## Cross-dept notes

- (operator: populate — which depts are upstream/downstream of this one?)

<!-- AUTOLINK-START — Obsidian wikilink graph backfill (idempotent; safe to re-run) -->

## References (auto-wikilinks)

- [[YYYY-MM-DD-training]]
- [[current-state]]
- [[decisions]]

<!-- AUTOLINK-END -->
