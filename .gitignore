# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*~

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node (in case lib/ helpers grow JS)
node_modules/

# Build / dist artifacts (Libro is clone-and-run; no build outputs ship)
dist/
build/
*.tar.gz
*.zip

# Operator profile — must never be committed (lives in ~/.cerebro/)
profile.yaml
.cerebro/

# Install runtime artifacts
.libro-backup-*/
.libro-manifest.json
*.log

# Agent-runtime local state (never ship — contains absolute host paths)
.claude-flow/
.swarm/
.hive-mind/

# Lint logs (run locally, not committed)
*-lint.log
